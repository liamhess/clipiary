## General
- option: launch on startup
- config: rework UI to match macOS common config style
- themes: pixel art effects 
- search: prefer exact match ("k ann")
- search: show the lines with match if not in first two lines